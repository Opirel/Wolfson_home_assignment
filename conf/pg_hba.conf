# TYPE  DATABASE     USER      ADDRESS           METHOD

# Allow local UNIX-socket connections with password (SCRAM)
local   all          all                         scram-sha-256

# Loopback from the host (if you published 5432:5432 for dev)
host    all          all       127.0.0.1/32      scram-sha-256
host    all          all       ::1/128           scram-sha-256

# Docker network (replace CIDR if your network differs)
host    womensdeptdb  all       172.18.0.0/16     scram-sha-256
host    womensdeptdb  all       172.19.0.0/16     scram-sha-256
